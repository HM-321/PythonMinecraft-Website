(()=>{const modal=document.createElement('div');modal.className='diagram-modal';modal.innerHTML='<button type="button" aria-label="閉じる">×</button><img alt="拡大した図">';document.body.append(modal);const image=modal.querySelector('img');const close=()=>{modal.classList.remove('open');image.removeAttribute('src')};document.addEventListener('click',e=>{const b=e.target.closest('.diagram-zoom');if(b){image.src=b.dataset.full;image.alt=b.querySelector('img')?.alt||'拡大した図';modal.classList.add('open')}else if(e.target===modal||e.target===modal.querySelector('button'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})})();
