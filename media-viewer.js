(()=>{const modal=document.createElement('div');modal.className='lightbox';modal.innerHTML='<button class="lightbox-close" aria-label="閉じる">×</button><img alt="拡大画像">';document.body.append(modal);const im=modal.querySelector('img');const close=()=>{modal.classList.remove('open');im.removeAttribute('src')};document.addEventListener('click',e=>{const target=e.target.closest('[data-zoom]');if(target){im.src=target.dataset.zoom;im.alt=target.querySelector('img')?.alt||'拡大画像';modal.classList.add('open')}else if(e.target===modal||e.target.closest('.lightbox-close'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})})();
