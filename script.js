const pages=[...document.querySelectorAll('.page')];const tabs=[...document.querySelectorAll('[data-index]')];const counter=document.querySelector('#counter');let current=0;function show(index){current=(index+pages.length)%pages.length;pages.forEach((page,i)=>page.classList.toggle('active',i===current));tabs.forEach((tab,i)=>tab.classList.toggle('active',i===current));counter.textContent=`${current+1} / ${pages.length}`;history.replaceState(null,'',`#page-${current}`);pages[current].scrollTop=0}tabs.forEach(tab=>tab.addEventListener('click',()=>show(Number(tab.dataset.index))));document.querySelector('#prev').addEventListener('click',()=>show(current-1));document.querySelector('#next').addEventListener('click',()=>show(current+1));document.addEventListener('keydown',event=>{if(['ArrowRight','PageDown'].includes(event.key))show(current+1);if(['ArrowLeft','PageUp'].includes(event.key))show(current-1)});const initial=pages.findIndex(page=>`#${page.id}`===location.hash);show(initial<0?0:initial);